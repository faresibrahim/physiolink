import 'package:flutter/material.dart';

abstract final class AppColors {
  // Brand
  static const Color primary = Color(0xFF004AAD);
  static const Color primaryLight = Color(0xFFE8F0FE);

  // Secondary / Success
  static const Color secondary = Color(0xFF4CAF82);
  static const Color secondaryLight = Color(0xFFE8F5EE);

  // Surfaces
  static const Color background = Color(0xFFF2F2F7);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF323232);
  static const Color textSecondary = Color(0xFF8E8E93);

  // Feedback
  static const Color destructive = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFF59E0B);

  // Structure
  static const Color divider = Color(0xFFE5E5EA);

  static const Color textHeading = Color(0xFF323232);
}
