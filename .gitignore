# OS
.DS_Store
Thumbs.db

# Zip artifacts
*.zip

# Local secrets — never commit
.env
secrets.md

# Claude Code local settings (per-machine permission allowlist, can embed
# local dev command strings/credentials) — never commit
.claude/settings.local.json
**/.claude/settings.local.json
